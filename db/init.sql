CREATE TABLE user_accounts (
    uniqname varchar primary key,
    userfirst varchar,
    userlast varchar,
    password varchar,
    funds float, );

insert into user_accounts values (1, cardib, cardi, belcalis, bodakyellow, 0);

